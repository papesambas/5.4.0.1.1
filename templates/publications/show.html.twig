{% extends 'blog/base.html.twig' %}

{% block title %}
	{{publication.titre}}
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mb-4">{{publication.titre}}</h1>
		<div class="row">
			<div class="col-md-4">
				<p>
					publié par :
					{{publication.author}}
				</p>
			</div>
			<div class="col-md-4">
				<p>
					le :
					{{ publication.createdAt ? publication.createdAt|date('Y-m-d H:i:s') : '' }}
				</p>
			</div>

		</div>
		<hr>
		<p>{{publication.contenu|raw}}</p>

	</div>


{% endblock %}
